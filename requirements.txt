# LegalLens Backend Dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9         # Required for FastAPI file uploads

# LangChain + LangGraph (the agent stack)
langchain==0.3.0
langchain-core==0.3.0
langchain-community==0.3.0
langgraph==0.2.0

# LLM providers (all free tier)
langchain-google-genai==2.0.0   # Gemini 2.5 Flash — primary LLM
langchain-groq==0.2.0           # Groq + Llama 3.3 70B — Q&A layer

# Embeddings + vector store
langchain-cohere==0.3.0         # Cohere Embed v3 — RAG embeddings
faiss-cpu==1.8.0                # FAISS in-memory vector store

# Document parsing
PyPDF2==3.0.1                   # PDF text extraction
python-docx==1.1.0              # DOCX text extraction

# Auth0 (JWT validation for protected routes)
python-jose[cryptography]==3.3.0
httpx==0.27.0                   # Async HTTP client for Auth0 JWKS

# Utilities
pydantic==2.8.0
python-dotenv==1.0.0
